@extends('layouts.app',['title'=>'Applications'])
@section('content')
<div class="page-head"><div><p class="eyebrow">Workload inventory</p><h1>Applications</h1><p class="muted">Seluruh workload yang dikelola Harbor Control.</p></div><a class="btn" href="{{ route('projects.create') }}">＋ NEW APPLICATION</a></div>
<form class="card filter-bar" method="get"><label>Runtime<select name="type"><option value="">ALL TYPES</option>@foreach(['static','laravel','vite'] as $type)<option value="{{ $type }}" @selected(request('type')===$type)>{{ strtoupper($type) }}</option>@endforeach</select></label><label>Status<select name="status"><option value="">ALL STATES</option>@foreach(['pending','deploying','running','stopped','failed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ strtoupper($status) }}</option>@endforeach</select></label><button>APPLY FILTER</button><a class="btn secondary" href="{{ route('applications.index') }}">RESET</a></form>
<div class="application-grid">@forelse($projects as $project)<article class="card app-card"><div class="app-head"><span class="app-icon">{{ strtoupper(substr($project->name,0,2)) }}</span><div><a href="{{ route('projects.show',$project) }}"><strong>{{ $project->name }}</strong></a><small>{{ strtoupper($project->type) }} · {{ $project->branch }}</small></div><span class="badge {{ $project->status }}">{{ $project->status }}</span></div><div class="app-meta"><span>DOMAIN<strong>{{ $project->primaryDomain?->domain ?? 'Not configured' }}</strong></span><span>LAST DEPLOY<strong>{{ $project->last_deployed_at?->diffForHumans() ?? 'Never' }}</strong></span><span>COMMIT<strong>{{ $project->last_commit ? substr($project->last_commit,0,8) : '--------' }}</strong></span></div><div class="actions"><a class="btn secondary" href="{{ route('projects.show',$project) }}">MANAGE</a><form method="post" action="{{ route('projects.deploy',$project) }}">@csrf<button>DEPLOY</button></form><a class="btn secondary" target="_blank" href="{{ route('projects.logs',$project) }}">LOGS</a></div></article>@empty<div class="card"><p>Belum ada application.</p><a class="btn" href="{{ route('projects.create') }}">DEPLOY FIRST PROJECT</a></div>@endforelse</div>
<style>.application-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(380px,1fr));gap:14px}.app-card{margin:0}.app-head{display:flex;gap:11px;align-items:center}.app-head>div{flex:1}.app-head strong{display:block;font-size:14px}.app-head small{display:block;color:var(--muted);font-size:9px;margin-top:2px}.app-icon{display:grid;place-items:center;width:37px;height:37px;background:#241017;border:1px solid #7d2632;color:#ff5361;font-weight:800}.app-meta{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:18px 0;padding:13px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.app-meta span{font-size:8px;color:#596779;letter-spacing:.8px}.app-meta strong{display:block;color:#aeb9c7;font-size:9px;margin-top:4px;overflow:hidden;text-overflow:ellipsis}@media(max-width:600px){.application-grid{grid-template-columns:1fr}.app-meta{grid-template-columns:1fr}}</style>
<style>.filter-bar{display:flex;align-items:end;gap:10px}.filter-bar label{min-width:160px}.filter-bar select{margin-bottom:0}@media(max-width:650px){.filter-bar{align-items:stretch;flex-direction:column}.filter-bar label{min-width:0}}</style>
@endsection
